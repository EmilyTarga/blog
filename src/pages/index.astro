---

const pathSegments = Astro.url.pathname.split('/').filter(Boolean); // Remove empty segments
const hasblog = pathSegments.find(segment => segment == "blog");

---

<meta http-equiv="refresh" content=`0;url=/blog/ptbr/` />
